namespace FlyingAzure.Core;

/// <summary>Where (if anywhere) to draw the date/time overlay on each monitor.</summary>
public enum ClockCorner
{
    Off,
    TopLeft,
    TopRight,
    BottomLeft,
    BottomRight,
}
